services:
  - name: app
    type: web
    env: docker
    runtime: image
    image:
      url: docker.io/linkace/linkace:simple  # Changed from :latest to :simple
    envVars:
      - key: PORT
        value: "80"
    plan: starter
    region: oregon
    disk:
      name: app-app-storage-app-backups
      mountPath: /app/storage/app/backups
      sizeGB: 10
