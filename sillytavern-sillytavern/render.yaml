services:
  - name: default
    type: web
    env: docker
    runtime: image
    image:
      url: docker.io/library/nginx:nginx
    startCommand: ""
    plan: starter
    region: oregon
    envVars:
      - key: PORT
        value: "80"
